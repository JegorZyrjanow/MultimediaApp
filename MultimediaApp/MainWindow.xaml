<Window x:Class="MultimediaApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultimediaApp"
        mc:Ignorable="d"
        Title="DegradationGrandMachine30000" Height="400" Width="600"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <!--SizeToContent="WidthAndHeight"-->
    <Grid>

        <TreeView x:Name="FolderView" VerticalAlignment="Top" FontFamily="Segoe UI Emoji" Margin="344,35,0,0">
            <TreeView.Resources>

                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Width="32" Height="32" Margin="3"
                                        Source="{Binding
                                            RelativeSource={RelativeSource
                                                Mode=FindAncestor,
                                                AncestorType={x:Type TreeViewItem}},
                                                Path=Tag,
                                                Converter={x:Static local:HeaderToImageConverter.Instance}}" />
                                    <TextBlock VerticalAlignment="Center" Text="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

            </TreeView.Resources>
        </TreeView>

        <Border BorderThickness="1" HorizontalAlignment="Left" Height="237" Margin="10,10,0,0" VerticalAlignment="Top" Width="327" Background="#FFBBBBBB"/>
        <Image x:Name="ImageBox" HorizontalAlignment="Left" Height="237" Margin="10,10,0,0" VerticalAlignment="Top" Width="327" Stretch="Fill"/>
        <Button x:Name="Save" Content="Save" HorizontalAlignment="Left" Margin="176,296,0,0" VerticalAlignment="Top" Width="161" Click="Save_Click"/>
        <Button x:Name="Download" Content="Load existing list" HorizontalAlignment="Left" Margin="10,296,0,0" VerticalAlignment="Top" Width="161" Click="Download_Click"/>
        <Button x:Name="AddPicture" Content="add new meme" HorizontalAlignment="Left" Margin="176,252,0,0" VerticalAlignment="Top" Width="161" Click="AddPicture_Click"/>
        <Button x:Name="DeletePicture" Content="Delete selected meme" HorizontalAlignment="Left" Margin="10,252,0,0" VerticalAlignment="Top" Width="161" Click="DeletePicture_Click"/>
        <TextBox x:Name="SearchBox" HorizontalAlignment="Left" Height="20" Margin="344,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="140" TextChanged="SearchBox_TextChanged"/>
        <ComboBox x:Name="CategoriesComboBox" HorizontalAlignment="Left" Height="20" Margin="489,10,0,0" VerticalAlignment="Top" Width="95" SelectionChanged="CategoriesComboBox_SelectionChanged" SelectedIndex="0"/>

    </Grid>
</Window>
